using UnityEngine;

namespace Proto2.Unit
{
    public class NewProtoTitan : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
